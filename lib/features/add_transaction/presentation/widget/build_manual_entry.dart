import 'package:flutter/material.dart';

class BuildManualEntry extends StatelessWidget {
  const BuildManualEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}